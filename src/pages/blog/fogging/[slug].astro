---
import { getCollection, render, type CollectionEntry } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import BlogSidebar from '../../../components/BlogSidebar.astro';

export async function getStaticPaths() {
  const posts = await getCollection('fogging');
  return posts.map(post => ({ params: { slug: post.id }, props: { post } }));
}

type Props = { post: CollectionEntry<'fogging'> };
const { post } = Astro.props;
const { Content } = await render(post);
const allPosts = await getCollection('fogging');
allPosts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

function formatDate(date: Date) {
  return date.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' });
}
---

<Layout title={`${post.data.title} · BL Blog`}>
  <canvas id="mist-canvas" aria-hidden="true"></canvas>
  <div class="entry-overlay" aria-hidden="true"></div>
  <div class="page-bg" aria-hidden="true">
    <div class="bg-orb bg-orb-1"></div>
    <div class="bg-orb bg-orb-2"></div>
  </div>

  <header class="top-bar reveal-fade" id="header">
    <a href="/" class="home-btn" aria-label="返回主页">
      <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7"
           stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M3 10L10 3l7 7M5 8.5V17h4v-4h2v4h4V8.5"/>
      </svg>
      <span>主页</span>
    </a>
    <button class="sb-toggle-btn" id="sb-toggle" aria-label="打开目录">
      <svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.7"
           stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <line x1="3" y1="6" x2="17" y2="6"/>
        <line x1="3" y1="10" x2="17" y2="10"/>
        <line x1="3" y1="14" x2="17" y2="14"/>
      </svg>
      <span>目录</span>
    </button>
  </header>

  <BlogSidebar posts={allPosts} currentSlug={post.id} basePath="/blog/fogging" categoryName="迷茫" />

  <main class="content-main reveal-fade" id="content">
    <article class="prose">
      <h1 class="article-title">{post.data.title}</h1>
      <div class="article-meta">
        <time>{formatDate(post.data.date)}</time>
        {post.data.tags.map(tag => <span class="meta-tag">{tag}</span>)}
      </div>
      <div class="article-divider" aria-hidden="true"></div>
      <div class="prose-body">
        <Content />
      </div>
    </article>
  </main>
</Layout>

<style>
  :root {
    --f1: #0466c8; --f2: #0353a4; --f3: #023e7d;
    --f4: #002855; --f5: #001845;
    --f6: #33415c; --f7: #5c677d; --f8: #7d8597; --f9: #979dac;
    --text-light: rgba(200, 220, 240, 0.92);
    --text-dim:   rgba(150, 175, 205, 0.62);
    --sb-bg:            rgba(1, 18, 48, 0.55);
    --sb-border:        rgba(4, 102, 200, 0.2);
    --sb-header-color:  rgba(4, 102, 200, 0.9);
    --sb-text:          rgba(200, 220, 240, 0.88);
    --sb-text-dim:      rgba(150, 175, 205, 0.55);
    --sb-tag-bg:        rgba(4, 102, 200, 0.12);
    --sb-tag-border:    rgba(4, 102, 200, 0.22);
    --sb-tag-text:      rgba(150, 200, 240, 0.8);
    --sb-active-bg:     rgba(4, 102, 200, 0.18);
    --sb-active-border: #0466c8;
    --sb-hover-bg:      rgba(4, 102, 200, 0.09);
    --sb-scrollbar:     rgba(4, 102, 200, 0.25);
  }

  #mist-canvas {
    position: fixed; inset: 0; z-index: 0; pointer-events: none;
  }

  .entry-overlay {
    position: fixed; inset: 0; z-index: 100;
    background: linear-gradient(160deg, var(--f5) 0%, var(--f3) 50%, var(--f1) 100%);
    pointer-events: none;
    animation: fogOut 0.9s cubic-bezier(0.87, 0, 0.13, 1) 0.2s forwards;
  }
  @keyframes fogOut {
    0%   { transform: translateY(0) skewY(0deg); }
    100% { transform: translateY(-112%) skewY(-4deg); }
  }

  .page-bg {
    position: fixed; inset: 0; z-index: -1;
    background: var(--f5); overflow: hidden;
  }
  .bg-orb { position: absolute; border-radius: 50%; filter: blur(100px); }
  .bg-orb-1 {
    width: 60vw; height: 60vw;
    background: radial-gradient(circle, var(--f1), var(--f3), transparent);
    top: -20%; right: -15%; opacity: 0.4;
    animation: oBob1 16s ease-in-out infinite;
  }
  .bg-orb-2 {
    width: 50vw; height: 50vw;
    background: radial-gradient(circle, var(--f6), var(--f5), transparent);
    bottom: -10%; left: -10%; opacity: 0.5;
    animation: oBob2 20s ease-in-out infinite;
  }
  @keyframes oBob1 { 0%,100% { transform: translate(0,0); } 50% { transform: translate(-5vw,7vh); } }
  @keyframes oBob2 { 0%,100% { transform: translate(0,0); } 50% { transform: translate(6vw,-5vh); } }

  .reveal-fade {
    opacity: 0; transform: translateY(16px);
    transition: opacity 0.7s cubic-bezier(0.16,1,0.3,1), transform 0.7s cubic-bezier(0.16,1,0.3,1);
  }
  .reveal-fade.visible { opacity: 1; transform: none; }

  .top-bar {
    position: fixed; top: 0; left: 0; right: 0; z-index: 50;
    display: flex; align-items: center; justify-content: space-between;
    padding: 1.2rem 1.8rem;
    background: rgba(0, 24, 69, 0.45);
    backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(4, 102, 200, 0.15);
  }

  .home-btn {
    display: flex; align-items: center; gap: .45rem;
    font-size: .85rem; font-weight: 500; letter-spacing: .04em;
    color: var(--text-dim);
    transition: color .25s ease, transform .25s ease;
  }
  .home-btn svg { width: 1.1rem; height: 1.1rem; }
  .home-btn:hover { color: var(--text-light); transform: translateX(-3px); }

  .sb-toggle-btn {
    display: none; align-items: center; gap: 0.4rem;
    padding: 0.4rem 0.9rem; font-size: 0.8rem; font-weight: 500; letter-spacing: 0.06em;
    color: var(--text-dim); background: rgba(4,102,200,0.1);
    border: 1px solid rgba(4,102,200,0.2); border-radius: 100px;
    cursor: pointer; transition: color 0.2s, background 0.2s;
  }
  .sb-toggle-btn svg { width: 1rem; height: 1rem; }
  .sb-toggle-btn:hover { color: var(--text-light); background: rgba(4,102,200,0.18); }

  .content-main {
    margin-left: 260px; min-height: 100svh;
    padding: 7rem 4rem 4rem 3.5rem;
    position: relative; z-index: 1;
    display: flex; justify-content: center;
  }

  .prose { width: 100%; max-width: 720px; }

  .article-title {
    font-size: clamp(1.6rem, 3.5vw, 2.4rem); font-weight: 700;
    color: var(--text-light); letter-spacing: -0.02em; line-height: 1.25;
    margin-bottom: 0.8rem;
  }

  .article-meta {
    display: flex; align-items: center; gap: 0.5rem;
    flex-wrap: wrap; margin-bottom: 1.5rem;
  }
  .article-meta time { font-size: 0.8rem; color: var(--text-dim); letter-spacing: 0.04em; }
  .meta-tag {
    font-size: 0.68rem; padding: 0.15rem 0.55rem;
    background: var(--sb-tag-bg); border: 1px solid var(--sb-tag-border);
    border-radius: 999px; color: var(--sb-tag-text);
  }

  .article-divider {
    height: 1px;
    background: linear-gradient(90deg, var(--sb-active-border), transparent);
    margin-bottom: 2rem; opacity: 0.45;
  }

  .prose-body { font-size: 1.05rem; line-height: 1.9; color: var(--text-dim); letter-spacing: 0.01em; }
  .prose-body :global(h2) { font-size: 1.45rem; font-weight: 600; color: var(--text-light); margin-top: 2.5rem; margin-bottom: 0.8rem; }
  .prose-body :global(h3) { font-size: 1.2rem; font-weight: 600; color: var(--text-light); margin-top: 2rem; margin-bottom: 0.6rem; }
  .prose-body :global(p) { margin-bottom: 1.4rem; }
  .prose-body :global(ul), .prose-body :global(ol) { padding-left: 1.5rem; margin-bottom: 1.4rem; }
  .prose-body :global(li) { margin-bottom: 0.4rem; }
  .prose-body :global(blockquote) {
    border-left: 3px solid var(--sb-active-border); padding: 0.5rem 1rem;
    margin: 1.5rem 0; opacity: 0.8; font-style: italic;
  }
  .prose-body :global(code) {
    background: rgba(255,255,255,0.08); padding: 0.15rem 0.4rem;
    border-radius: 4px; font-size: 0.88em;
  }
  .prose-body :global(pre) {
    background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.1);
    border-radius: 10px; padding: 1.2rem 1.5rem; overflow-x: auto; margin-bottom: 1.5rem;
  }
  .prose-body :global(pre code) { background: none; padding: 0; }
  .prose-body :global(a) { color: var(--sb-active-border); text-decoration: underline; text-underline-offset: 3px; }
  .prose-body :global(hr) { border: none; border-top: 1px solid rgba(255,255,255,0.12); margin: 2.5rem 0; }
  .prose-body :global(strong) { color: var(--text-light); font-weight: 600; }
  .prose-body :global(img) { max-width: 100%; border-radius: 8px; margin: 1rem 0; }

  @media (max-width: 860px) {
    .content-main { margin-left: 0; padding: 6rem 1.5rem 3rem; }
    .sb-toggle-btn { display: flex; }
  }
  @media (max-width: 560px) { .article-title { font-size: 1.5rem; } }
</style>

<script>
  const canvas = document.getElementById('mist-canvas') as HTMLCanvasElement;
  const ctx    = canvas.getContext('2d')!;
  function resize() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; }
  resize(); window.addEventListener('resize', resize);

  type Phase = 'in' | 'hold' | 'out';
  interface Particle {
    x:number;y:number;r:number;opacity:number;maxOpacity:number;
    vx:number;vy:number;life:number;maxLife:number;phase:Phase;
  }

  function spawn(initial=false): Particle {
    const r = Math.random()*140+55;
    let x:number, y:number;
    if (initial) { x=Math.random()*canvas.width; y=Math.random()*canvas.height; }
    else {
      const edge=Math.floor(Math.random()*4);
      x=edge===0?-r:edge===1?canvas.width+r:Math.random()*canvas.width;
      y=edge===2?-r:edge===3?canvas.height+r:Math.random()*canvas.height;
    }
    const maxLife=Math.random()*450+280;
    return {
      x,y,r,opacity:initial?Math.random()*0.06:0,maxOpacity:Math.random()*0.13+0.03,
      vx:(Math.random()-0.5)*0.3,vy:(Math.random()-0.5)*0.2,
      life:initial?Math.floor(maxLife*0.35*Math.random()):0,maxLife,
      phase:initial?'hold':'in',
    };
  }

  const particles: Particle[] = Array.from({length:65},()=>spawn(true));

  function drawP(p:Particle) {
    const g=ctx.createRadialGradient(p.x,p.y,0,p.x,p.y,p.r);
    g.addColorStop(0,`rgba(165,190,215,${p.opacity})`);
    g.addColorStop(0.5,`rgba(135,160,195,${p.opacity*0.55})`);
    g.addColorStop(1,`rgba(100,135,170,0)`);
    ctx.fillStyle=g; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill();
  }

  function updateP(p:Particle) {
    p.x+=p.vx; p.y+=p.vy; p.life++;
    if(p.phase==='in'){p.opacity+=0.0018;if(p.opacity>=p.maxOpacity){p.opacity=p.maxOpacity;p.phase='hold';}}
    else if(p.phase==='hold'&&p.life>p.maxLife*0.68){p.phase='out';}
    else if(p.phase==='out'){p.opacity-=0.0012;if(p.opacity<=0)Object.assign(p,spawn(false));}
  }

  (function loop(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    particles.forEach(p=>{updateP(p);drawP(p);});
    requestAnimationFrame(loop);
  })();

  const ov=document.querySelector('.entry-overlay') as HTMLElement;
  ov?.addEventListener('animationend',()=>ov.remove(),{once:true});

  const DELAY=1050;
  setTimeout(()=>document.getElementById('header')?.classList.add('visible'),DELAY);
  setTimeout(()=>document.getElementById('content')?.classList.add('visible'),DELAY+200);
</script>
